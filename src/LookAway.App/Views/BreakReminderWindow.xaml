<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="LookAway.App.Views.BreakReminderWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RequestedTheme="Light" Padding="32" RowSpacing="16" Background="{StaticResource RcBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            x:Name="TitleText"
            Grid.Row="0"
            Style="{StaticResource RcH1TextStyle}" />

        <TextBlock
            x:Name="HintText"
            Grid.Row="1"
            FontSize="{StaticResource RcFontSizeH3}" />

        <TextBlock
            x:Name="CountdownText"
            Grid.Row="2"
            TextWrapping="NoWrap"
            VerticalAlignment="Bottom"
            HorizontalAlignment="Left"
            Foreground="{StaticResource RcTextSecondary}"
            Visibility="Collapsed" />

        <StackPanel
            Grid.Row="3"
            Orientation="Horizontal"
            Spacing="12"
            HorizontalAlignment="Right">
            <Button x:Name="StartButton" Click="OnStartBreakClick" Style="{StaticResource RcPrimaryButtonStyle}" />
            <Button x:Name="SnoozeButton" Click="OnSnoozeClick" />
            <Button x:Name="SkipButton" Click="OnSkipClick" />
        </StackPanel>
    </Grid>
</Window>
