# Screenshots

Aufnahmen der Oberfläche für die READMEs. PNG, 800 px Breite, deutschsprachige Oberfläche.

Vorhanden:

- `settings.png` – Einstellungen, Bereich „Allgemein"
- `hotkeys.png` – Einstellungen, Bereich „Hotkeys" mit der Neubelegung
- `statistics.png` – Statistik-Ansicht (Zahlen einer frischen Installation)

Nicht vorhanden:

- Tray-Icon mit Tooltip und das Erinnerungsfenster. Beide erscheinen nur im laufenden Betrieb
  und ließen sich nicht ohne Bedienung am Bildschirm auslösen. Sie werden nachgereicht, sobald
  sie sich aufnehmen lassen — und nicht nachgestellt.

Aufgenommen wird das Fenster über `PrintWindow` aus seinem eigenen Puffer, nicht als
Bildschirmausschnitt: Sonst landet alles mit im Bild, was zufällig davor liegt.
